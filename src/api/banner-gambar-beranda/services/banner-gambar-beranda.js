'use strict';

/**
 * banner-gambar-beranda service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::banner-gambar-beranda.banner-gambar-beranda');
