'use strict';

/**
 * banner-gambar-beranda router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::banner-gambar-beranda.banner-gambar-beranda');
